<Popup x:Class="Ai.Hong.Controls.ColorPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200" Opened="Popup_Opened">
    <Popup.Resources>
        <Style TargetType="{x:Type Border}"  x:Key="rectStyle">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="true">
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Width" Value="17"/>
                    <Setter Property="Height" Value="17"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="false">
                    <Setter Property="Margin" Value="1"/>
                    <Setter Property="Width" Value="15"/>
                    <Setter Property="Height" Value="15"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Popup.Resources>
    <UniformGrid Rows="7" Columns="5" Name="rootGrid" Background="White">
    </UniformGrid>
</Popup>
